pipeline {
    agent any
    options {
    	timestamps()
    }
    stages {
      
    	stage("build"){
			steps {
    		echo "build"
			}
    	}
    	stage("test"){
			steps {
    		echo "test"
			}
    	}
    	stage("deploy"){
			steps{
    		echo "deploy"
			}
    	}
    }
}