pipeline {
    agent any
    options {
    	timestamps()
    }
    stages {
      
    	stage("build"){
    		echo "build"
    	}
    	stage("test"){
    		echo "test"
    	}
    	stage("deploy"){
    		echo "deploy"
    	}
    }
}